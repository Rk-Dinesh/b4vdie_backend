Backend Development - Hackwit Technologies Pvt Ltd

Create the Project Folder and Naming the folder with Prject Name folled by backend (demo_backend)

Then, Start open the folder in IDE or terminal and follow the instuction below.


1 . Set up the Node JS project and Environment by typing the following command.

>> npm init
Give your project Name
>> npm install express mongoose body-parser
>> npm install bcrypt
>> npm install nodemon
>> npm install jsonwebtoken

2. Create the Files & Folder structure given below

 2.1. create the `index.js` and `app.js` Files
 2.2. create `config`, `model`,`services` , `controller`and `routers` folders
 2.3. navigate to `config` folder and create `db.js` file
    2.3.1. create the database strings and configuration in the `db.js` file
 2.4. 

